use yew::prelude::*;

#[function_component(CameraSetup)]
pub fn camera_setup() -> Html {
    html! {
        <div class="camera-setup">
            {"Camera setup will allow users to take pictures of their plants."}
        </div>
    }
}

