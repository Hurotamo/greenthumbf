use yew::prelude::*;

#[function_component(ReferralProgram)]
pub fn referral_program() -> Html {
    html! {
        <div class="referral-program">
            {"Referral program details and code for sharing."}
        </div>
    }
}

